<Window x:Class="IntentionalExpression.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IntentionalExpression"
        mc:Ignorable="d"
        Title="Intentional Expression" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        
        <!-- Region Row 0, Content -->
        <Canvas Name="ShowHere" Grid.Row="0" />
        <!-- EndRegion -->
        
        <!--  Region Row 1, Buttons -->
        <Button Grid.Row="1" Content="Test" Background="DarkGreen" Foreground="White" />
        <!-- EndRegion -->
    </Grid>
</Window>
